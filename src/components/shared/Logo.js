import React from "react";
import styled from "@emotion/styled";

const LogoText = styled.h1`
  font-family: 'Pacifico', cursive;
  font-size: 8rem;
  color: #fff;
  margin: 0;
  text-align: center;
`;

const Logo = () => {
  return <LogoText>Tried In EGYPT</LogoText>;
};

export default Logo;